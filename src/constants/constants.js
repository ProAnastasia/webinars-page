/**
 * enum for modal types
 * @type {string}
 */
export const ModalName = {
  FORM: 'form'
};

/**
 * enum for storage items names
 * @type {string}
 */
export const StorageItemName = {
  WEBINARS: 'webinars'
};

/**
 * enum for key names according to codes
 * @type {number}
 */
export const KeyName = {
  ESC: 27
};

/**
 * enum for number of items for webinars render
 * @type {Number}
 */
export const RenderListLimit = {
  INITIAL: 8
};